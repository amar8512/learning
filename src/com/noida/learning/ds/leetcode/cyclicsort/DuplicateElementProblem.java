package com.noida.learning.ds.leetcode.cyclicsort;

import java.util.ArrayList;
import java.util.Arrays;
//import java.util.HashSet;
//import java.util.Set;

public class DuplicateElementProblem {
	
	int x;
	
	public static void main(String args[])
	{
	
	ArrayList al =new ArrayList();
	al.add(3);
	
	al.add(2);
	
	al.get(2);
	}

	@Override
	public boolean equals(Object obj) {
		// TODO Auto-generated method stub
		return super.equals(obj);
	}
	
	
	/*public static void main(String args[]) {
		//Set s1 = new HashSet();
		boolean flag = false;
		int arr[] = { 8, 2, 1, 8, 0, 1, 9, 0, 7, 5, 2, };
		Arrays.sort(arr);
		int value = 10;
		int i = 0;
		if (i == 0 && arr[i] == arr[i + 1]) {
			flag = true;
		} else {
			for (i = 1; i < arr.length; i++) {
				if ((arr[i] == arr[i - 1]) || (arr[i] == arr[i + 1])) {
					flag = true;
				}
			}
		}

		if (flag) {
			System.out.println("The element " + value + " has duplicates");
		} else {
			System.out.println("Duplecate does not exist for " + value);
		}
	}
*/
}
